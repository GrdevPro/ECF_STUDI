{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>ReparationMoteur</th>
                <td>{{ service.reparationMoteur }}</td>
            </tr>
            <tr>
                <th>ReparationCarosserie</th>
                <td>{{ service.reparationCarosserie }}</td>
            </tr>
            <tr>
                <th>VenteVehicule</th>
                <td>{{ service.venteVehicule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_add_services_index') }}">back to list</a>

    <a href="{{ path('app_admin_add_services_edit', {'id': service.id}) }}">edit</a>

    {{ include('admin_add_services/_delete_form.html.twig') }}
{% endblock %}
